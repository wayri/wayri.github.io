version: '3.8'

services:
  jekyll:
    build: .
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/site
      - jekyll_bundle:/usr/local/bundle
    environment:
      - JEKYLL_ENV=development
    restart: unless-stopped

volumes:
  jekyll_bundle:
